import Module_url
def main():
    Module_url.input_url(input('Введите полную ссылку>'),input('Введите сокращенную ссылку>'))
    Module_url.input_url(input('Введите полную ссылку>'),input('Введите сокращенную ссылку>'))
    Module_url.search_url(input('Введите сокращенную ссылку для получения полной>'))
if __name__ == '__main__':
    main()